## Check your understanding:
Question 1: I would fit my automated tests within a Github action that runs whenever code is pushed. This is because it automatically checks whether or not the new code breaks the existing project before it is merged. Github actions ensures the tests are running properly every time the code is pushed.

Question 2: I would not use an end to end test to check if a function is returning the correct output. Instead, I would use a unit test because they focus on testing individual functions and smaller parts of the code. End to end testing is more suitable for checking if the entire application works correctly from the user's perspective.




